# sources/ (gitignored)

Raw AI-chat exports and conversation dumps land here — `chatgpt-YYYY-MM/`, etc. They are processed ONCE: distilled into `01-foundation/` files with `source:` frontmatter provenance, then never read by any model again. Nothing in this folder is ever committed (only this README travels with the repo). Process the ore; commit the metal; don't enshrine the ore.
